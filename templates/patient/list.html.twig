{% extends 'base.html.twig' %}

{% block content %}


<div class="example-wrapper">
  list patient: <br/>
    <table  id="dtBasicExample" class="table table-striped table-bordered table-sm" cellspacing="0" >
      {% for item in Patients %}
        <tr>
          <td>
              <a href="#">{{item.nom}}</a>
          </td>
          <td>
              <a href="#">{{item.prenom}}</a>
          </td>
          <td>
              <a href="#">add info</a>
          </td>

        </tr>
      {% endfor %}
    </table>

</div>
{% endblock %}
